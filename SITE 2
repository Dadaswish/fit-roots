<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>FITROOTS Sea Moss</title>
<style>
  body {
    font-family: 'Poppins', sans-serif;
    background-color: #121212;
    color: #f0f0f0;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    margin: 0;
  }

  .product-card {
    background-color: #1e1e1e;
    border-radius: 15px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
    max-width: 400px;
    padding: 25px;
    text-align: center;
    transition: transform 0.3s ease;
  }

  .product-card:hover {
    transform: scale(1.02);
  }

  .product-card img {
    width: 100%;
    border-radius: 15px;
  }

  .product-name {
    font-size: 1.5rem;
    margin: 15px 0 5px;
    color: #8bc34a;
    letter-spacing: 1px;
  }

  .price {
    font-size: 1.3rem;
    color: #ffd54f;
    margin-bottom: 15px;
  }

  .options, .quantity {
    margin: 10px 0;
  }

  select, input[type="number"] {
    padding: 10px;
    border-radius: 8px;
    border: none;
    outline: none;
    width: 100%;
    background-color: #2c2c2c;
    color: #f0f0f0;
  }

  button {
    margin-top: 20px;
    background-color: #8bc34a;
    color: #121212;
    border: none;
    padding: 12px 20px;
    font-size: 1rem;
    border-radius: 8px;
    cursor: pointer;
    width: 100%;
    transition: 0.3s;
  }

  button:hover {
    background-color: #9ccc65;
  }

  .price-display {
    margin-top: 10px;
    font-size: 1.2rem;
    color: #ffd54f;
  }
</style>
</head>
<body>

<div class="product-card">
  <img src="IMG_7816.png" alt="FITROOTS Sea Moss Gel">
  <h2 class="product-name">FITROOTS Sea Moss Gel</h2>
  <p>Breathe Deep. Revive Naturally.</p>

  <div class="options">
    <label for="size">Choose Size:</label>
    <select id="size" onchange="updatePrice()">
      <option value="28.99">8oz Jar - $28.99</option>
      <option value="47.99">16oz Jar - $47.99</option>
    </select>
  </div>

  <div class="quantity">
    <label for="quantity">Quantity:</label>
    <input type="number" id="quantity" value="1" min="1" onchange="updatePrice()">
  </div>

  <div class="price-display">
    Total: $<span id="total">28.99</span>
  </div>

  <button onclick="addToCart()">Add to Cart</button>
</div>

<script>
function updatePrice() {
  const size = parseFloat(document.getElementById('size').value);
  const qty = parseInt(document.getElementById('quantity').value);
  const total = (size * qty).toFixed(2);
  document.getElementById('total').innerText = total;
}

function addToCart() {
  const sizeText = document.getElementById('size').selectedOptions[0].text;
  const qty = document.getElementById('quantity').value;
  const total = document.getElementById('total').innerText;
  alert(`${qty} × ${sizeText} added to cart (Total: $${total})`);
  // 🔗 Later, you can replace this alert with Shopify checkout link
}
</script>

</body>
</html>
